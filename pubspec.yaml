name: foodpanda_clone
description: Flutter port of the Lovable foodpanda-style demo.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  go_router: ^14.2.0
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true
